# terraform-prometheus-operator-crds

Terraform module to deploy Prometheus Operator CRDs.


## Usage

```hcl
module "prometheus_operator_crds" {
  source = "github.com/claranet/terraform-prometheus-operator-crds?ref=v0.80.0"
}
```
